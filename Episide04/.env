###
# Required 
###

OPENAI_API_KEY=your_openai_api_key_here

# Where to store extracted text from the PDF's, audio files, etc.
TRANSCRIPT_DIR=./transcripts


###
# Optional
###

# FAISS index persistence (defaults to "faiss_index" in current directory)
# This is where the local vector database will be saved, if Pinecone is not configured below
FAISS_INDEX_PATH=./faiss-index

# Pinecone configuration (for cloud vector storage)
# If these two variables are both set, it will not use FAISS and will choose Pinecone instead
# PINECONE_API_KEY=your_pinecone_api_key_here
# PINECONE_INDEX=your_index_name


# Model configuration 

# Whisper model options: tiny, base, small, medium, large, large-v2, large-v3
WHISPER_MODEL=base

# OpenAI embedding model options: text-embedding-3-small, text-embedding-3-large
OPENAI_EMBEDDING_MODEL=text-embedding-3-small

# OpenAI model options: gpt-3.5-turbo, gpt-4, gpt-4-turbo, gpt-4o, etc.
OPENAI_MODEL=gpt-4o
